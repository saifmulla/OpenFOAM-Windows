language: cpp
os:
    - linux
    - osx
compiler:
    - g++
    - clang++
branches:
    only:
        - master
        - devel
        - ubuntu-fix
script:
    - mkdir _build && cd _build
    - cmake ../
    - make
    - make test
