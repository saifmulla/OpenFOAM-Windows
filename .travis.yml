language: cpp
os:
    - linux
    - osx
compiler:
    - g++
    - clang++
branches:
    only:
        - master
        - devel
        - move-tests
        - openfoam-shared-lib
script:
    - mkdir _build && cd _build
    - cmake ../
    - make
    - make test
